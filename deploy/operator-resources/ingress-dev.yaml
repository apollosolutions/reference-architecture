apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: router
  namespace: apollo
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: reference-architecture-dev
                port:
                  number: 80


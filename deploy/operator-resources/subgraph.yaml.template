apiVersion: apollographql.com/v1alpha2
kind: Subgraph
metadata:
  name: ${SUBGRAPH_NAME}
  namespace: ${SUBGRAPH_NAME}
  labels:
    app: ${SUBGRAPH_NAME}
    apollo.io/subgraph: "true"
spec:
  endpoint: http://graphql.${SUBGRAPH_NAME}.svc.cluster.local:4001
  schema:
    sdl: |
${SCHEMA_CONTENT}


apiVersion: v1
kind: Service
metadata:
  name: {{ include "promotions-api.fullname" . }}
  labels:
    {{- include "promotions-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "promotions-api.selectorLabels" . | nindent 4 }}
